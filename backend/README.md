# HomeSync Backend (deprecated)

This FastAPI backend has been replaced by direct Supabase JS client calls in the frontend + a Deno Edge Function for shopping sync. It is kept here for reference but is no longer used.
